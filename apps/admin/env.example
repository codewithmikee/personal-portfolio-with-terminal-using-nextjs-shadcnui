# Database Configuration
# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database?schema=public

# For local development (using Docker or local PostgreSQL)
DATABASE_URL="postgresql://portfolio_user:portfolio_password@localhost:5432/portfolio_db?schema=public"

# For production (Vercel Postgres, Supabase, etc.)
# DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# API Configuration
NEXT_PUBLIC_API_URL="http://localhost:3000"

# Optional: Analytics
NEXT_PUBLIC_VERCEL_ANALYTICS_ID=""

# Optional: Development settings
NODE_ENV="development"

# Shadow database for migrations (optional, for development)
# SHADOW_DATABASE_URL="postgresql://username:password@host:port/shadow_db?schema=public"
